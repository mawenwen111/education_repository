server:
  port: 7005
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka
  instance:
    instance-id: education-freeMarker
    prefer-ip-address: true
spring:
  application:
    name: education-freeMarker
    #rabbitmq配置信息
  rabbitmq:
    host: 192.168.211.130
  datasource:
    url: jdbc:mysql://localhost:3306/education_demo?characterEncoding=UTF8&serverTimezone=GMT%2B8
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
  main:
    allow-bean-definition-overriding: true